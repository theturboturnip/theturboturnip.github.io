<!doctype html>
<html>
	<head>
	<meta charset="utf-8">
	<meta name="viewport" content="width=device-width, user-scalable=no, minimal-ui">
	<title>CV - {{#resume.basics}}{{name}}{{/resume.basics}}</title>
	<link href="https://maxcdn.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css" rel="stylesheet">
	<style>
		{{{css}}}
	</style>
	</head>
	<body>
		<main id="resume" class="page">
			{{#if resume.special}}
			{{> special }}
			{{else}}
			{{> resume-header}}
			<div class="resume-content">
				<aside class="about-header">
					{{> about }}
					{{> skills }}
					{{> languages }}
					{{> interests }}
				</aside>
				<div class="resume-contents">
					{{> summary}}
					{{> education }}
					{{> work }}
					{{> volunteer }}
					{{> project }}
					{{> awards }}
					{{> publications }}
					{{> references }}
				</div>
			</div>
			{{/if}}
		</main>
	</body>
</html>
